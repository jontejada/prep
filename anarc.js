var anarchy = function () {
  var one =  Math.ceil(Math.random());
  var two = one + one;
  var three = two + one;
  return 'I can write numbers like, '+ one +', '+ two +', '+ three +'.';
}

console.log(anarchy());